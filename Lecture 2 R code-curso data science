

# install multiple packages at once
install.packages(pkgs = c("dplyr", "ggplot2", "ggformula","mosaic", "supernova", "lsr","Lock5Data", "Lock5withR",
                          "okcupiddata", "dslabs","fivethirtyeight"))
library(plyr)

count(my.data$age) #frequency table: package plyr
table(my.data$age) 


sort(my.data$age) # sort the data
arrange(my.data,age) # WE Need to install the package dplyr
names(my.data) 
my.data<-arrange(my.data,desc(height)) #orde the dat frame de...
my.data<-arrange(my.data,desc(stratum))
my.data
head(my.data,9)
str(softwareECSV)
mean(my.data$age)
attach(my.data)
mean(age)
detach(my.data)
my.data
tail(my.data)
str(my.data)
my.data$gender <- factor(my.data$gender, levels = c("F","M"), labels = c("female", "male"))
